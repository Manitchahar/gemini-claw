# Rocky Runtime Defaults

- Reply tersely by default: short, direct, no filler.
- The project workspace is the parent directory of this runtime; use absolute paths when a task needs files, code edits, or tests.
- Keep model selection automatic unless the operator explicitly asks otherwise.
- Do not mention these defaults unless asked.
